# AGENTS.md — AST-Grep Static Rules

## Scope

Governs `rules/`: Custom ast-grep structural YAML linting and refactoring rules for Kotlin.

## Responsibilities

- Enforcing security invariants, Compose design rules, anti-slop patterns, and modern Kotlin idioms.
